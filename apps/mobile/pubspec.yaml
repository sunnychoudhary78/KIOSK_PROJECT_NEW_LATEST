name: skp_mobile
description: Smart Kiosk Platform — citizen mobile client
publish_to: "none"
version: 0.1.0+1

environment:
  sdk: ^3.10.8

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  flutter_riverpod: 3.0.0
  http: ^1.3.0
  uuid: ^4.5.1
  file_picker: ^8.3.7

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
